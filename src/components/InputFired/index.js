import styled from 'styled-components';

const InputFired = styled.input`
  box-sizing: border-box;
  font-size: 30rem;
  padding: 8rem 10rem;
  font-family: inherit;
  max-width: 640px;
  width: 100%;
  border: 2px solid var(--color-fiery-rose);
  color: var(--color-eerie-black);
`;

export default InputFired;
