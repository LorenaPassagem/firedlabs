import styled from 'styled-components';
import logo from '../../assets/img/Logo.png';

const LogoFired = styled.img.attrs({ src: logo, alt: 'Logo da FiredLabs' })`
  height: 40px;
`;

export default LogoFired;
