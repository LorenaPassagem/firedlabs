import styled from 'styled-components';
import logoFired from '../../assets/img/LogoFired.svg';

const LogoFired = styled.img.attrs({
  src: logoFired,
  alt: 'Logo da FiredLabs',
})``;

export default LogoFired;
